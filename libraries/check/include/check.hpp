#ifndef CHECK_HPP
#define CHECK_HPP

namespace Check
{
	bool checkPrime(int number);

	bool isEven(int n);

	bool isOdd(int n);

	bool isZero(int number);

	bool checkMark(float mark, float passMark = 50.0);

	bool isValidDimension(double dimension);
};

#endif